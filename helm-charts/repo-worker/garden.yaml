kind: Module
name: repo-worker
type: helm
build:
  dependencies:
    - repo-worker-ctr
valueFiles:
  - values.yaml
values:
  image:
    repository: ${modules.repo-worker-ctr.outputs.deployment-image-name}
    tag: ${modules.repo-worker-ctr.version}
  ingress:
    host: ${var.hostname}
